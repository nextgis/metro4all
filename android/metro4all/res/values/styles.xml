<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="Theme.Application" parent="@style/Theme.AppCompat.Light">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorPrimaryInverse">@color/metrocolormid</item>
        <item name="actionMenuTextColor">@color/metrocolormid</item>
    </style>

    <style name="Theme.Settings" parent="@style/Theme.Application">
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="toolbarStyle">@style/Widget.Toolbar</item>
    </style>

    <style name="Widget.Toolbar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:background">?attr/colorPrimary</item>
        <item name="navigationIcon">?attr/homeAsUpIndicator</item>
        <item name="android:textColorPrimary">@color/metrocolormid</item>
        <item name="android:textColorPrimaryInverse">@color/metrocolormid</item>
        <item name="actionMenuTextColor">@color/metrocolormid</item>
        <item name="android:textColor">@color/metrocolormid</item>
        <item name="colorPrimary">@color/metrocolormid</item>
        <item name="colorAccent">@color/metrocolormid</item>
        <item name="colorPrimaryDark">@color/metrocolormid</item>
    </style>

</resources>
